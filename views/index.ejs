<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: linear-gradient(135deg, #74ebd5 0%, #9face6 100%);
      min-height: 100dvh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      color: #333;
    }

    .container {
      background: #fff;
      padding: 24px;
      border-radius: 16px;
      box-shadow: 0px 8px 20px rgba(0,0,0,0.15);
      text-align: center;
      max-width: 480px;   /* Default mobile width */
      width: 100%;
      transition: all 0.3s ease;
    }

    h1 {
      margin-bottom: 12px;
      font-size: 1.5rem;
      color: #444;
    }

    p {
      margin-bottom: 24px;
      font-size: 1rem;
      color: #666;
      line-height: 1.4;
    }

    .btn-container {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }

    .btn {
      display: block;
      text-decoration: none;
      padding: 14px;
      border-radius: 12px;
      font-size: 1rem;
      font-weight: 600;
      transition: all 0.3s ease;
      text-align: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .btn.upload {
      background: #4CAF50;
      color: white;
    }
    .btn.upload:hover {
      background: #45a049;
    }

    .btn.view {
      background: #2196F3;
      color: white;
    }
    .btn.view:hover {
      background: #1976D2;
    }

    /* 📱 Mobile screens */
    @media (max-width: 600px) {
      .container {
        max-width: 100%;
        padding: 20px;
      }
      h1 {
        font-size: 1.4rem;
      }
      p {
        font-size: 0.95rem;
      }
      .btn {
        width: 100%;
        font-size: 1rem;
      }
    }

    /* 📟 Tablet screens */
    @media (min-width: 601px) and (max-width: 1023px) {
      .container {
        max-width: 700px;
        padding: 40px;
      }
      h1 {
        font-size: 1.8rem;
      }
      p {
        font-size: 1.05rem;
      }
      .btn-container {
        flex-direction: row;
        gap: 20px;
      }
      .btn {
        flex: 1;
        font-size: 1.05rem;
      }
    }

    /* 🖥 Large screens */
    @media (min-width: 1024px) {
      .container {
        max-width: 900px;
        padding: 60px;
      }
      h1 {
        font-size: 2.2rem;
      }
      p {
        font-size: 1.2rem;
      }
      .btn-container {
        flex-direction: row;
        gap: 30px;
      }
      .btn {
        flex: 1;
        font-size: 1.1rem;
        padding: 18px;
      }
    }

    /* Flash messages */
    .flash {
      position: fixed;
      top: 10px;
      left: 50%;
      transform: translateX(-50%);
      padding: 12px 20px;
      border-radius: 8px;
      font-size: 0.9rem;
      font-weight: 600;
      color: #fff;
      box-shadow: 0px 4px 12px rgba(0,0,0,0.2);
      z-index: 1000;
      opacity: 1;
      transition: opacity 0.6s ease;
      max-width: 90%;
      text-align: center;
    }

    .flash.success { background: #4CAF50; }
    .flash.error { background: #f44336; }
    .flash.hide { opacity: 0; }
  </style>
</head>
<body>
  <div>
    <% if (success && success.length > 0) { %>
      <div id="flash-message" class="flash success"><%= success %></div>
    <% } %>
    <% if (error && error.length > 0) { %>
      <div id="flash-message" class="flash error"><%= error %></div>
    <% } %>
  </div>

  <div class="container">
    <h1><%= title %></h1>
    <p>You can upload and access all your medical documents easily.</p>
    <div class="btn-container">
      <a href="/upload" class="btn upload">➕ Upload New Document</a>
      <a href="/documents" class="btn view">📂 View All Documents</a>
    </div>
  </div>

  <script>
    // Auto dismiss flash message
    setTimeout(() => {
      const msg = document.getElementById("flash-message");
      if (msg) {
        msg.classList.add("hide");
        setTimeout(() => msg.remove(), 600);
      }
    }, 3000);
  </script>
</body>
</html>
